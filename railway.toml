[build]
builder = "paketobuildpacks/builder:base"

[deploy]
start = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
node_env = "production"

[paths]
# Tell Railway your backend folder
root = "microfinance-backend"
